import GroupCircleIcon from "../Icons/GrupoCircleIcon";
import styles from "./Oferts.module.css";

const Ofers = () => {
  return (
    <figure className={styles.ofersContainer}>
      <GroupCircleIcon className={styles.groupIcons} />
    </figure>
  );
};

export default Ofers;